node_modules
.pnpm-store
dist
coverage
.env
.env.*
!.env.example
.DS_Store
.vscode
.idea
.netlify
supabase/.temp
.turbo
*.tsbuildinfo

# Stray Git-for-Windows install folder that ended up inside the project
# directory (not part of the project - see chat for context). Ignored
# rather than deleted from here since it's large and outside git's remit.
/Git/
